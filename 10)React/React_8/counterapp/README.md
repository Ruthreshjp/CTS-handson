Counter App - React hands-on lab

Instructions

1. Install dependencies:

```bash
npm install
```

2. Start development server:

```bash
npm start
```

This creates a small app with a `CountPeople` class component that tracks `entrycount` and `exitcount` in component state. Click `Login` to increment entries and `Exit` to increment exits.
